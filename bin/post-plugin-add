#! /usr/bin/env bash

. ../lib/utils.sh

printf "\

asdf-nodejs: You might also be interested in setting up auto-reshimming for npm
global installs. Check more about it in https://github.com/asdf-vm/asdf-nodejs#install
or just add the following lines into your .bashrc/.zshrc file

    asdf list $(plugin_name) > /dev/null 2>&1 && \\
        PATH=\"\$(asdf $(plugin_name) wrappers-path):\$PATH\"\n"
